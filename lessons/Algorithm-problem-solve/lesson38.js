// first item in Array

function getFirstItem(yourItem) {
    return yourItem[0];
}

const age = [6, 4, 3, 2];
console.log(getFirstItem(age));

const name = ['Tasib', 'Rafi', 'Rakib'];
console.log(getFirstItem(name));

const me = 'Tasib';
console.log(getFirstItem(me));